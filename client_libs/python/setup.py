from setuptools import setup, find_packages

setup(
    name='hackMatrix',
    version='0.1',
    author='collinalexbell',
    packages=["hackMatrix", "hackMatrix.protos"],
    install_requires=[
        # List your dependencies here
    ],
)
